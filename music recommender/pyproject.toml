[project]
name = "mood_recommender"
version = "1.0.0"
description = "Console version of Mood-Based Music Recommender"
authors = [{name="Your Name"}]
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
